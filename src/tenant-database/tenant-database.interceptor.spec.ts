import { TenantDatabaseInterceptor } from './tenant-database.interceptor';

describe('TenantDatabaseInterceptor', () => {
  it('should be defined', () => {
    expect(new TenantDatabaseInterceptor()).toBeDefined();
  });
});
